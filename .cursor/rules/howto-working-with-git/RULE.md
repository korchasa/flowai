---
alwaysApply: true
---
## HOW TO WORK WITH GIT

- Always use git commands only with `GIT_PAGER=cat` env variable. For example, `GIT_PAGER=cat git diff`, `GIT_PAGER=cat git log`, `GIT_PAGER=cat git show`.
- Use [commit-changes.mdc](mdc:.cursor/rules/commit-changes.mdc) for commit rules