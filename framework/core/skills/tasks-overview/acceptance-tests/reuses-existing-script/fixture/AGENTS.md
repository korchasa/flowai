# Project rules — tasks

This project keeps its tasks as flat files in `tasks/<slug>.md` (not the `documents/tasks/<YYYY>/<MM>/` layout described above, which is superseded here). Frontmatter `status: open | done | dropped`; `done` and `dropped` are archived. Progress lives in a `## Steps` checklist.
