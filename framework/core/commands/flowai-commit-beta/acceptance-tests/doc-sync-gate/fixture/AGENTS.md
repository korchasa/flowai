# Project: CliTool

A sample CLI utility.

## Documentation Hierarchy

1. `AGENTS.md` — project rules (this file).
2. `documents/requirements.md` — SRS (functional requirements).
3. `documents/design.md` — SDS (architecture and components).
4. `README.md` — public-facing overview and usage.

## Documentation Rules

- Every user-visible change (CLI flag, command, option) MUST be reflected in SRS + README.
- Changed behavior of existing component → update SDS component section.
- Documentation is the only link between sessions — stale docs actively mislead.
