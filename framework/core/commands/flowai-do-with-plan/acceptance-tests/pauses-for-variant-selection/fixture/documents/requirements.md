# SRS

## 3. Functional Requirements

### FR-RETRY: Configurable Retry Policy

- **Description:** http_client.ts must support a configurable retry policy (count + backoff strategy).
- **Acceptance:** `deno test -A` passes the retry test suite.
- **Status:** [ ]
